## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-23T13:24:27Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | — | (carried, not re-litigated per instructions) | Prior finding, previously Resolved. | Verified the revision did not reintroduce the underlying defect (see Summary). | Resolved |
| R-02 | — | (carried, not re-litigated per instructions) | Prior finding, previously Resolved. | Verified the revision did not reintroduce the underlying defect (see Summary). | Resolved |
| R-03 | — | (carried, not re-litigated per instructions) | Prior finding, previously Resolved. | Verified the revision did not reintroduce the underlying defect (see Summary). | Resolved |
| R-04 | — | (carried, not re-litigated per instructions) | Prior finding, previously Resolved. | Verified the revision did not reintroduce the underlying defect (see Summary). | Resolved |
| R-05 | Critical | `functional-spec.md` rule BR11.6 (`logic`, `scope_of_the_hit_target_clause`), cross-checked against `frontend-components.md` § "What each earlier unit must keep providing" row "U3 rows", and against U3's `functional-spec.md` BR9.2 | BR11.6's operative `logic` clause no longer reads as a universal "one hit target" rule. It was rewritten to the invariant "a row does not GAIN a hit target on contraction" — which is vacuously true for a Projects row, since Projects rows carry two targets at *every* width (verified directly in U3's `functional-spec.md`: BR9.2 states "A Projects list row carries exactly two targets... at every width", not contingent on breakpoint). A new `scope_of_the_hit_target_clause` field states the Projects-row exception explicitly, names BR9.2 and BR11.1 by ID, and forbids reading BR11.6 as an instruction to merge the outbound link's target. `frontend-components.md`'s "U3 rows" dependency row was independently updated to state the same reconciliation ("by design, at every width... not as a contraction artefact... BR11.6's clause is scoped so it does not reach these rows") and matches BR11.6's text exactly — the two artifacts no longer disagree. The reconciliation with `requirements.md` NFR6 is legitimate rather than an excuse to depart from it: NFR6's verification criterion ("list rows keep one hit target with the date moving beneath the title") is a description of the *contraction transformation* for the row types that had one target at desktop (Writing/Home), which is exactly what the rewritten clause still forbids violating; NFR6 was written before Projects rows (BR9.2, added at U3) or BR11.1 existed, so it never actually contemplated a row with two by-design targets, and the scoping documents that gap rather than quietly reversing NFR6. This is a genuine fix, not prose added alongside an unchanged universal clause: the offending words ("a list row keeps ONE hit target... rather than the row splitting into separate targets," applied without qualification) are gone from the rule text itself. | None — contradiction removed, verified against both this unit's and U3's authored text. | Resolved |
| R-06 | Major | `traceability.json` coverage row `{"id":"NFR1","status":"OK","target":"BR11.7, BR5.2, BR11.5"}`, cross-checked against `requirements.md` NFR1 and the new `functional-spec.md` rule BR11.7 | BR11.7 is a genuine new rule with real `logic` and `on_violation` fields (not a restatement deferring to the walkthrough): it obligates that "visual order matches document order for every focusable element," bans specific layout techniques that would violate that (positive `order` on focusable children, absolute-position reordering, float/direction tricks), and separately bans `outline: none` without an equal-or-better replacement and removing the indicator on `:focus-visible`. That is a real, checkable statement of NFR1 conditions (2) tab order and (3) no focus trap — the defect class R-06 named (an assumption or walkthrough step standing in for a rule) is fixed. `on_violation` states plainly that nothing automated detects a violation and that W2 steps 3-5 are "the entire verification" — the no-automated-check caveat from R-05/BR11.5's pattern is preserved honestly, not concealed. U5A4 was reframed from an assumption into a note recording its promotion to BR11.7, worded accurately ("unverified by a tool" and "unstated as a rule are different things, and only the first is true now") — this does not overclaim verification, only that the obligation now exists as a rule. The NFR1 coverage row's three targets (BR11.7 for conditions 2-4, BR5.2 for condition 1) are each independently traceable to rule text that actually states the corresponding obligation; BR11.5 (contrast re-measurement) is a weaker fit for NFR1 but is additive to a row that is otherwise fully and honestly justified, so it is not grounds to fail this on its own. | None — genuine rule added, coverage row is now truthful for the two previously-ungrounded conditions. | Resolved |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| `aidlc engine sensor-traceability --output-path <u5 traceability.json> --stage-slug functional-design` | `pass: false`; `invalid_targets` names every `BRx.y` target in the U5 coverage rows (`BR11.5`, `BR11.7`, `BR5.2`, `BR11.3`, `BR11.4`, `BR5.10`, `BR11.6`, `BR11.1`) as `"absent from rules.md"`; `missing_from_upstream_ids` lists FR1–FR5.5; `reason: "required upstream artifact is missing: .../rules.md"` | Confirms the new "Where this unit's rules live" H2's two disclosed claims exactly. Read the stage definition directly: `produces_kinds.rules: [service, spec, library]` excludes `ui` — U5 is correctly not required to produce `rules.md`, so `invalid_targets` firing here is the sensor disagreeing with the stage contract, as the artifact states. Read the sensor source (`aidlc-sensor-traceability.ts`, `resolveUpstream`, `stage === "functional-design"` branch): the per-unit scope (via `unit-of-work-story-map.md` + `stories.md`) only activates `if (existsSync(stories) && existsSync(storyMap))`; since `inception/user-stories/` was not run this scope, it falls through to `idsFromFile(requirements, [ID_PATTERNS.FR], ...)`, pulling every `FR*` ID unscoped — matching the run's output (only FR IDs, no NFR IDs, appear in `missing_from_upstream_ids`). Both disclosed claims are accurate. One minor imprecision: the artifact's prose says the check compares "against the whole requirement set," but the branch taken for `functional-design` only pulls `FR*` IDs (not `NFR*`) when stories are absent — the sensor output itself corroborates this (no NFR ID appears in the list). This is a wording looseness, not a misstatement that would mislead a developer relying on the disclosure; not raised as a new finding. |

### Summary

Both open findings from the prior iteration are resolved by verifiable text changes, cross-checked directly against the two named sibling files (U3's `functional-spec.md` BR9.2 and `frontend-components.md`'s "U3 rows" row) and against the requirements-analysis source for NFR1/NFR6. BR11.6's rewritten invariant ("does not gain a hit target on contraction") is vacuously satisfied by Projects rows rather than merely being narrated around by the new scope field, so the contradiction with BR11.1/BR9.2 is actually removed, not just annotated. BR11.7 is a real rule with logic and on_violation, not a restatement of the walkthrough, and the NFR1 coverage row is now truthful for tab order and focus-trap avoidance. The new disclosure H2 was independently verified against the traceability sensor's live output and the stage definition's `produces_kinds`, and both of its claims hold. R-01 through R-04 remain intact — the revision touched only BR11.6, BR11.7, U5A4, the "U3 rows" dependency row, the disclosure H2, and the summary tables, none of which reopens ground those findings covered. No new Critical or Major defect was found in this pass.

## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-24T19:25:32Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | `construction/u5-visual-direction/code-generation/traceability.json` > `NFR1` and `BR11.7` coverage rows; `code-generation-plan.md` Step 1 "Traces:" line | Both rows attach the Step 1 change (`.project-rail a` underlined at rest, fixing a colour-only distinction) to NFR1 and BR11.7. Read literally, neither rule establishes that commitment: `requirements.md` line 164 defines NFR1 exhaustively as four things — the skip link, tab order, no focus trap, and a visible focus indicator — none of which is "a link must not be distinguished from adjacent text by colour alone." `functional-spec.md` line 309 defines BR11.7 as "styling never moves a focusable element out of its document position, and never removes or suppresses a focus indicator" — again nothing about colour-only signalling. The actual source for the commitment being fixed is `design-system-mapping.md`'s own contrast principle (quoted correctly in the plan, "must never be the only thing conveying a boundary"), which is not a BR/NFR ID at all and carries no verification story of its own (NFR1's and BR11.7's own verification is the keyboard walkthrough, which does not examine link colour). Filing the fix under NFR1/BR11.7 makes it look like an established, walkthrough-verified obligation when it is actually an uncovered design commitment whose only guard is `tests/u5/stylesheet.test.ts` test 5 — exactly the retargeting pattern `project.md` § Corrections already names ("A traceability row marked OK asserts that some rule establishes every commitment the requirement makes... When no rule does, author the missing rule rather than retargeting the row to a nearer one"). | Either author a rule (in the unit's own rule set or as a named upstream gap) that actually states "a link must carry a non-colour signal, not colour alone," and trace to it, or record this commitment in `traceability.json` as resting on `design-system-mapping.md` directly with an explicit note that no BR/NFR currently owns it and no walkthrough step checks it. | New |
| R-02 | Major | `construction/u5-visual-direction/code-generation/traceability.json` > `NFR6` coverage row; `code-generation-plan.md` Step 2 "Traces:" line | The Step 2 change (`overflow-wrap: anywhere` on `.prose`) is traced to NFR6. `requirements.md` line 169 defines NFR6's acceptance criteria exhaustively as three named behaviours — the rail stacking, list rows keeping one hit target, and the top bar staying visible with no hamburger. A long URL overflowing the reading column is not one of the three, and NFR6's own verification method ("load each page type at a desktop width and at a phone width; each stated behaviour holds") does not mention link/word wrapping. The plan's justification ("breaks NFR6's 'the phone layout is the desktop layout contracted' in the most visible way available") quotes NFR6's introductory framing, not its enumerated, checkable criteria. This is a real and worthwhile fix, but it is not actually established by NFR6 as written, so filing it there overstates what NFR6 covers and understates that this fix currently rests on nothing but this pass's own test 6. | Trace this fix to the actual source (NFR6's general framing, explicitly flagged as broader than the three enumerated behaviours, or a new rule) rather than the enumerated NFR6 acceptance criteria as if the URL-overflow case were already one of the three named behaviours. | New |
| R-03 | Minor | `tests/u5/stylesheet.test.ts` lines 209–211 (test "underlines `.project-rail a` at rest, not only on hover") | The second assertion (`const hover = ruleBody(css, ".project-rail a:hover"); expect(hover ?? "").not.toContain(...)`) is vacuous against the current stylesheet: `ruleBody` returns `undefined` when no such selector exists (confirmed — there is no `.project-rail a:hover` rule anywhere in `site.css`), so `hover ?? ""` is `""` and the assertion passes trivially regardless of whether a hover rule is ever reintroduced with different wording. The regression protection this test actually provides comes entirely from its first assertion (the rest rule contains the underline); the second assertion does not add coverage of "not confined to hover" beyond that. `code-summary.md` and `unit-test-instructions.md` both describe test 5 as asserting the underline "is not confined to a `:hover` selector," which overstates what the second half of the test does. | Either drop the second assertion as redundant, or make it meaningful (e.g., assert that no rule with selector `.project-rail a:hover` exists at all, rather than checking the content of a rule that may not exist) and correct the description in `code-summary.md`/`unit-test-instructions.md` if it is kept as-is. | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| `npx tsc --noEmit` | Exit 0, no output | Clean, matches `code-summary.md`'s claim |
| `npx eslint .` | Exit 0, no output | Clean, matches `code-summary.md`'s claim |
| `npx prettier --check .` | Exit 0, "All matched files use Prettier code style!" | Clean, matches `code-summary.md`'s claim |
| `npx vitest run tests/u1 tests/u2 tests/u3 tests/u4 tests/u5` | 22 files, 160 tests, all pass, exit 0 | Matches `code-summary.md`'s recorded figure exactly |

Per the plan's § How this pass is reviewed, `npx vitest run … --coverage` and `node --import tsx bin/check.ts` were **not** re-run (they write `coverage/`, `dist/`, `.build/` into the workspace root that fingerprints this receipt). Their recorded output in `code-summary.md` was scrutinised instead:
- Coverage run: 96.25% lines (540/561) against the inherited 80% floor, all 160 tests passing — plausible given the four-tool re-run above reproduced the same 160-test, 22-file count.
- `vitest.config.ts` was read directly: `thresholds: { lines: 80 }` at line 11, unchanged; no `coverage.exclude` entry present anywhere in the file. Confirms the floor was not weakened.
- `bin/check.ts` recorded output (3/3 site checks pass) was not independently reproduced per the review contract; no contradicting evidence found elsewhere.

### Summary

The differential claim holds up under inspection: `source-manifest.json` names exactly `src/assets/styles/site.css` and `tests/u5/stylesheet.test.ts`, both changes described in the plan are present and nothing else in the stylesheet appears altered, the dead `.project-rail a:hover` rule is genuinely gone, the `↗` outbound-mark and no-live-URL claims in `code-summary.md` were independently verified against `src/page-renderer/pages.ts` and `content/projects/`, and the two new tests would genuinely fail against the pre-fix stylesheet (confirmed by static trace of `ruleBody`/`rootDeclarations` semantics) — so they test the claimed change rather than passing vacuously, modulo R-03. The main defect is that both of this pass's stylesheet changes are traced in `traceability.json` to BR/NFR IDs that, read literally against their own definitions in `requirements.md` and `functional-spec.md`, do not establish the commitments being fixed — an instance of the exact retargeting anti-pattern the team already corrected against once in this project. That is a real, checkable inaccuracy in a stage-contract artifact, but it does not indicate the underlying code or tests are wrong, and at two Major findings the unit still clears the READY bar.

## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-24T12:43:15Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Critical | `construction/u4-about-page/code-generation/traceability.json` > coverage row `NFR1` | `requirements.md` line 164 defines `NFR1` as: "Every page shall meet WCAG 2.1 AA for keyboard operation and landmark structure: a skip-to-content link as the first focusable element, tab order following visual order, no focus trap, and a visible focus indicator on every focusable element," verified by "The manual keyboard walkthrough… run when that page type is built." `code-generation-plan.md` Step 9 (the About-page keyboard walkthrough) is explicitly **unticked**, and `code-summary.md` states it "is not done." Yet `traceability.json` marks the row literally named `NFR1` as `status: "OK"`, with the note "Assertions read the written HTML file, so the page is proven complete in the served markup" — a claim about page-completeness (NFR2's territory), not about keyboard operation or landmark structure at all. No test in `tests/u4/about.test.ts` or `tests/u4/integration.test.ts` exercises tab order, focus visibility, skip-link behaviour, or focus traps. A separate, non-canonical ID invented for this same pass (`WCAG-2.1-AA-keyboard`) does correctly carry `PARTIAL` with an accurate note about the walkthrough being outstanding — but that does not cure the `NFR1` row: any reader or downstream gate trusting the canonical requirement ID `NFR1` (the ID actually used in `requirements.md` and cited as `## Mandated` in `project.md`) is told the mandated WCAG bar is fully satisfied for this page when the unit's own plan says its only verification step has not run. This is exactly the "NFR marked OK against a test that does not actually assert it" defect. | Change the `NFR1` coverage row's `status` to `PARTIAL` (matching the honest `WCAG-2.1-AA-keyboard` row) with a note that names the outstanding manual walkthrough, or fold the two rows into one canonical `NFR1` entry so there is exactly one status for this requirement and it is accurate. Do not leave a row under the real requirement ID claiming `OK` for content it does not verify. | New |
| R-02 | Minor | `src/page-renderer/pages.ts` lines 1-8 (module doc comment) | The module-level doc comment still reads "About carries one short placeholder paragraph that U4 replaces; the prose is throwaway by design," which is false now that `renderAbout` carries real, final-form prose. `code-summary.md` § Concerns handed forward discloses this honestly and states it was left alone because the unit's write scope is `renderAbout`'s body and its own doc comment, not the module banner. Confirmed unresolved by direct read of the file. | No fix required within this unit given the disclosed scope boundary; carry the correction forward explicitly so the next unit touching this file does not have to rediscover it (already stated in `code-summary.md`, so this is a confirmation rather than a new discovery). | New |
| R-03 | Minor | `package.json` > `"test"` script; disclosed in `code-generation-plan.md`/`code-summary.md` § Concerns handed forward | `"test": "vitest run tests/u1 tests/u2 --coverage"` omits both `tests/u3` and `tests/u4`, confirmed by direct read. Anyone running the literal `npm test` command gets a green 80%-line-floor result measured against a partial suite, silently under-measuring coverage. `package.json` is correctly outside this unit's `source-manifest.json` write list, and the gap is honestly disclosed rather than hidden. | No action required within U4; flagged here so Build and Test does not treat it as a novel discovery, and because it interacts with the org-mandated 80% floor being "verified… before merge" — a stale `test` script is the mechanism by which that verification could silently regress. | New |

### Validation Tool Results

| Tool / Command | Result | Interpretation |
|---|---|---|
| `npx vitest run tests/u4 tests/u1 tests/u2 tests/u3` (no coverage flag, per the no-write constraint) | 19 test files, 141 tests, all pass | Confirms the delivered test suite and the rest of the unit-scoped suites are green together; no regression introduced |
| Read-only inspection of existing `coverage/coverage-summary.json` (not regenerated) | Overall lines 95.68%, `src/page-renderer/pages.ts` 100% lines/statements/functions/branches | Confirms the 80% org-mandated floor is met by the existing measured artifact and `renderAbout` is fully exercised; `vitest.config.ts` still carries `thresholds: { lines: 80 }` unweakened (read directly) |
| Read-only inspection of existing `dist/about/index.html` (not rebuilt) | Exactly one `<h1>About</h1>`, followed by one `<h2>Elsewhere</h2>`, no heading skip; both contact links present with destination-naming visible text; CSP meta tag present; no third-party `href`/`src` at page load; no HTML comments | Confirms BR10.1–BR10.3, the CSP mandate, and the no-third-party-request mandate against the actual built artifact, not just the source |

### Verification of the builder's self-disclosed items

1. **The `siteName` deviation from `frontend-components.md` § Props and branches** — Verified directly against both files: `frontend-components.md` lists `AboutPage | — | none`; `renderAbout(siteName: string)` uses `siteName` only inside the head `description` string (`` `Who ${siteName} is, and where else to find them.` ``), never in `main`. No branch exists anywhere in the function body (confirmed by direct read of `src/page-renderer/pages.ts` lines 339-356). The plan's argument holds and is recorded as a decision (U4CA5), not a discrepancy.
2. **BR10.1/BR10.2/BR10.3 in the actual rendered artifact** — Confirmed against `dist/about/index.html`: prose lives in the template (no `content/pages/` file exists for it), both links are plain anchors named for their destination, and the page is emitted unconditionally per the source (no `if` around the page's presence in `site-builder.ts` line 229).
3. **The contact address `rue.asha@proton.me`** — Matches `code-generation-questions.md` Q2 follow-up answer exactly, matches the source (`pages.ts` line 353) and the built HTML exactly. No newsletter, subscriber list, or signup form exists anywhere in the delivered files or the built output — it is a single plain `mailto:` anchor, which `project.md` § Forbidden's newsletter/subscriber-list ban does not reach.
4. **`source-manifest.json` completeness** — All five declared files exist on disk at the declared paths, and `find` over the unit's actual on-disk footprint (`tests/u4/*`, the one modified `src/page-renderer/pages.ts`) turned up no additional U4-owned path missing from the manifest.
5. **Heading structure and no third-party resource claims** — Independently re-derived from the built `dist/about/index.html` rather than taken from the artifact's own claims (see Validation Tool Results above); both hold.

### Summary

The implementation itself is solid — the deviation is genuine and well-argued, the rendered markup and the real build both satisfy BR10.1–BR10.3 and the forbidden-newsletter/no-third-party-request mandates, and the test suite exercises the actual artifact rather than the drafted prose. The blocking defect is in `traceability.json`: the canonical requirement ID `NFR1` (the mandated WCAG 2.1 AA keyboard/landmark bar) is marked `OK` against a note that describes a different requirement's content, while the unit's own plan and code summary say in the same breath that the keyboard walkthrough — `NFR1`'s own stated verification method — has not been performed. A second, non-canonical ID invented for this pass correctly carries the honest `PARTIAL` status, but it does not fix the false `OK` sitting under the real ID, which is what a downstream gate or a future audit would actually query.

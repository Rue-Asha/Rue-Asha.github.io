## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-23T23:57:27Z
**Iteration:** 2

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | `entities.md` \> `Post.date` attribute | `entities.md` declared a `min`/`max` bound on `Post.date` that `BR2.3` did not enforce. | Spot-checked: `entities.md` line 156-160 now states "The `min`/`max` bound below is enforced by BR2.3, as `Project.year`'s bound is enforced by BR3.2" and `rules.md` \> `BR2.3` (lines 202-228) now enforces exactly that range \("IF the date is earlier than `1970-01-01`, or later than the build's own date plus 366 days, THEN field error"\). Confirmed still resolved. | Resolved |
| R-02 | Major | `functional-spec.md` \> W1 step 11 | W1 step 11's field-error aggregation omitted step 3 \(slug validation\), so a malformed slug might never reach the fail-loudly aggregate. | Spot-checked: `functional-spec.md` line 62 now reads "Collect every field error from steps 2, 3, 4, 7, 9 and 10", and lines 77-84 add an explicit paragraph explaining why step 3 belongs in the aggregate. Confirmed still resolved. | Resolved |
| R-03 | Minor | `rules.md` \> BR7.1-BR7.3 \> `entities.md` \> § Additions | `BR7.1`-`BR7.3` assigned to a "Publishing route" actor absent from `components.md`, unflagged in `entities.md` § Additions. | Spot-checked: `entities.md` line 453 \("`Publishing route` \(a rule actor, not a component and not an entity\)."\) under § Additions this stage makes \(line 431\), and `rules.md` \> § Where the BR7 rules are enforced \(lines 837-885\) carries the same flag. Confirmed still resolved. | Resolved |
| R-04 | Minor | `frontend-components.md` \> PostPage / § What this unit builds and a later unit completes | `frontend-components.md`'s `PostPage` fully implements FR2.8 \(top+bottom "All posts" links\) though FR2.8 belongs to u2, with no note anywhere. | Verified: lines 203-212 add the cross-unit note immediately after the table at line 183, naming U2 as the owner of FR2.8, its acceptance check, and its wording/placement; and confirmed `traceability.json`'s `upstream_ids` and `coverage` arrays do not list FR2.8 anywhere. Confirmed resolved as claimed. | Resolved |
| R-05 | Major | `traceability.json` \> FR4.3 / FR4.6 rows \> `rules.md` \> BR5.12 | FR4.3 requires Home to render an intro, a recent-posts section and a selected-projects section. The `status: OK` row targeted rules covering only the two sections — no rule established that Home carries an intro at all. | Verified against `requirements.md` line 128 \(FR4.3\) and line 131 \(FR4.6\). FR4.3's commitments are: an intro exists, a recent-posts section, a selected-projects section, and the two sections carry equal treatment. New rule `BR5.12` \(rules.md lines 618-648\) establishes the intro's existence, its position above both sections, its content source \(`SiteMetadata.siteName` as the sole `h1` plus one-or-two sentences of prose authored once outside `content/`, never derived from `Post`/`Project`\), and that it renders unchanged regardless of list state — together with `BR5.3` \(the two sections' contents\) and `BR5.4` \(their structural equality\), every clause of FR4.3 is now covered by some rule; the row is legitimately `OK`. For FR4.6 \("render Home showing its intro and both empty states... when neither posts nor projects exist"\), the widened target `BR5.3, BR5.5, BR5.12` is not an over-claim: BR5.3 states the empty-list branch triggers BR5.5's empty-state treatment, BR5.5 supplies the sentence-and-link content, and BR5.12 explicitly states "It renders unchanged when both sections are full, when one is empty, and when both are empty — the launch-day case" — the exact scenario FR4.6 names. The rule-count line \("Forty-five rules"\) matches an actual count of 45 `- id: BR` entries and 45 summary-table rows; no ID was renumbered \(BR5.12 appended at the end of the BR5 series\); the `yaml` fence parses cleanly via `yaml.safe_load`. FA6 \(intro wording left as an assumption\) and FOQ2 \(contact links routed to U4\) are both present in `functional-spec.md` § Assumptions & Open Questions exactly as claimed. No gap found. | Resolved |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| YAML fence parse (`rules.md`, `entities.md`) | PASS — both fenced `yaml` blocks parse via `yaml.safe_load`; `rules.md` block yields exactly 45 rule entries | Confirms the rule count line and no structural corruption from the R-05 edit |
| Rule-ID count cross-check (`grep -oE '^\s*- id: BR[0-9.]+'` vs summary table rows) | PASS — 45 IDs in the YAML block, 45 rows in the summary table, all matching, sequential, no renumbering | Confirms the "Forty-five rules" claim and that BR5.12 was appended rather than inserted disruptively |
| `traceability.json` JSON parse and cross-reference check (every `target` rule ID resolves against `rules.md`) | PASS — `json.load` succeeds; zero missing rule references across `coverage` and used targets | Confirms no dangling references were introduced by the FR4.3/FR4.6 retargeting |
| `requirements.md` FR4.3/FR4.6 wording vs. `BR5.3`/`BR5.4`/`BR5.5`/`BR5.12` coverage | PASS (manual verification) | Every commitment in both requirement statements is now backed by a rule; the widened FR4.6 row is not an over-claim |

### Summary

Both structural fixes from iteration 1 (R-01, R-02) and both minor documentation gaps (R-03, R-04) survive spot-check unchanged. The major R-05 gap — Home's intro having no enforcing rule — is closed by a well-scoped new rule `BR5.12` that covers the intro's existence, placement, content-sourcing discipline, and empty-state invariance; the retargeted FR4.3 and FR4.6 traceability rows are verified against the actual requirement wording and both are legitimate `OK` rows, not near-miss retargeting. No new findings. This is the final iteration; the artifact is READY.

## Review

**Verdict:** NOT-READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-23T23:43:19Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | `entities.md` § Entity Model \> `Post.date` attribute; `rules.md` \> `BR2.3` | Prior finding: `entities.md` declared a `min`/`max` bound on `Post.date` with no enforcing rule. Verified fixed: `BR2.3`'s logic block now states "IF the date is earlier than `1970-01-01`, or later than the build's own date plus 366 days, THEN field error naming file and `date`", `entities.md`'s constraint text for `Post.date` explicitly says "The `min`/`max` bound below is enforced by BR2.3, as `Project.year`'s bound is enforced by BR3.2", and `rules.md` § Rules that carry a cost worth stating gives the rationale for the new bound. The bound and its enforcement now match. | None — resolved. | Resolved |
| R-02 | Major | `functional-spec.md` § W1 \> step 11 | Prior finding: step 11's error aggregation omitted step 3 (slug validation), so a malformed slug might never reach the fail-loudly report. Verified fixed: step 11 now reads "Collect every field error from steps 2, 3, 4, 7, 9 and 10", and the paragraph immediately below the table ("Step 11's list of sources is exhaustive, and step 3 belongs in it...") explains why step 3 must be included and states the six sources are exhaustive with a rule for adding future ones. | None — resolved. | Resolved |
| R-03 | Minor | `entities.md` § Additions this stage makes, stated openly; `rules.md` § Where the BR7 rules are enforced | Prior finding: BR7.1–BR7.3 are assigned to a "Publishing route" actor absent from the 8-component catalogue, and not flagged in `entities.md` § Additions. Verified fixed: `applies_to` for BR7.1–BR7.3 now reads "Publishing route (not a component — see § Where the BR7 rules are enforced)"; `rules.md` adds a full § Where the BR7 rules are enforced section quoting `components.md` § Rationale verbatim and stating the real cost (not verifiable from `src/`, not covered by the three blocking checks, verified instead by U1's own definition-of-done conditions 1/2/4); `entities.md` § Additions now carries a third entry for "Publishing route (a rule actor, not a component and not an entity)" cross-referencing the same section. The divergence is honestly stated with a concrete, checkable cost rather than glossed over. | None — resolved. | Resolved |
| R-04 | Minor | `frontend-components.md` § Hierarchy (`PostPage` row) and § What this unit builds and a later unit completes (`PostPage` row) | Not claimed fixed, and it is not fixed: the hierarchy still shows `PostPage: PostHeader + AllPostsLink + RenderedBody + AllPostsLink` (both the top and end-of-body "All posts" link — the exact FR2.8 behaviour), and the completion table still lists `PostPage`'s "Built here" column as "Structure, head metadata, a minimal rendered body" with no mention of the AllPostsLink pair. FR2.8 is assigned to U2 in `unit-of-work-story-map.md` (line 80, under "U2 — Blog"), not to U1, and neither `functional-spec.md` nor the cross-unit completion tables in `entities.md`/`frontend-components.md` note this as a pulled-forward piece of U2's scope. A developer reading only the completion table would not know this duplication exists or that it belongs to another unit's requirement. | Either remove the duplicate `AllPostsLink` from U1's `PostPage` (deferring both links to U2, consistent with the "minimal rendered body" framing) or add an explicit note in the completion table / cross-cutting table stating that U1 implements FR2.8 early and why, so the FR is traceable to the unit that actually builds it. | Unresolved |
| R-05 | Major | `traceability.json` \> coverage row `{"id": "FR4.3", "target": "BR5.3, BR5.4, BR3.6, BR3.7"}`; `rules.md` § Rules (BR5.3, BR5.4) | FR4.3 states: "The system shall render Home with an intro, a recent-posts section, and a selected-projects section, giving both sections the same heading level, the same rule treatment, and the same \\"All X\\" affordance." The traceability row is marked `OK` against BR5.3 (count/fill/ordering for the two sections) and BR5.4 (structural equality between the two sections), which together do establish the recent-posts/selected-projects behaviour and the equal-treatment commitment. But no rule in `rules.md` establishes that Home carries an **intro** at all — `intro` does not appear anywhere in `rules.md`'s 44 rules, only in `frontend-components.md`'s `HomePage` hierarchy (`IntroBlock + SectionBlock(Writing) + SectionBlock(Projects)`), which is a structural design note, not a business rule with a stated violation behaviour. `project.md` § Corrections already records the exact learning this violates: "A traceability row marked `OK` asserts that some rule establishes every commitment the requirement makes, not merely that the unit works on that requirement. When no rule does, author the missing rule rather than retargeting the row to a nearer one." | Author a rule (e.g. a new `BR5.x`) that states Home always renders an intro block, in what circumstances (independent of post/project counts), and what a missing or empty intro would mean, then add it to the FR4.3 traceability target. Alternatively, if the intro is considered pure layout with no verifiable behaviour, state that explicitly in `rules.md` and justify why FR4.3's "intro" clause needs no rule — do not leave the traceability row silently short of the commitment. | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| YAML fence parse (entities.md `yaml` block) | PASS — `yaml.safe_load` parses all 6 entities without error | Confirms the `CheckReport.internalLinkResult` quoting issue named in the dispatch brief is fixed; the `constraint` field is now inside a `>` block scalar, not a bare leading backtick |
| YAML fence parse (rules.md `yaml` block) | PASS — `yaml.safe_load` parses all 44 rules without error | No structural YAML regressions introduced by the revision |
| grep for "FR2.8" / "All posts" across functional-spec.md | No hits | Confirms R-04's observation that the pulled-forward FR2.8 behaviour in `frontend-components.md` is not cross-referenced anywhere else in the unit's artifacts |
| grep for "intro" across rules.md and functional-spec.md | No hits | Confirms R-05: no business rule or workflow step establishes Home's intro block, despite FR4.3 naming it as a first-class commitment |

### Summary

Three of the four prior findings (R-01, R-02, R-03) are genuinely and carefully fixed, each with an explicit cross-reference tying the fix back to the original gap. R-04 remains open exactly as before — the FR2.8 duplication in `PostPage` still isn't attributed anywhere. More significantly, the new FR4.3 traceability claim this pass was asked to test hard does not survive scrutiny: BR5.3 and BR5.4 cover the count/ordering/equal-treatment commitments well, but the requirement's "intro" clause is asserted `OK` against rules that never mention it, reproducing the precise traceability failure mode the project's own corrections log had already flagged as unacceptable. One Major (new) and one Minor (carried, unresolved) finding remain, which is enough to hold the gate.

## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-24T19:44:09Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | `construction/u6-launch-content/functional-design/rules.md` § "The rules that govern this unit's files" (table), cross-checked against `code-generation/traceability.json` (`coverage[FR3.4]`, `reverse[BR3.4]`) and `code-generation/code-summary.md` (§ Key decisions / § Step A4) | This pass's `traceability.json` cites **U1 BR3.5** as the rule that specifies the live-URL rail-row omission the Homelab write-up exercises (both in the `FR3.4` coverage entry and the `BR3.4` reverse entry), and `code-summary.md` repeats the same citation twice. But `rules.md`'s own table of "the rules that govern this unit's files" — which `functional-spec.md` § "How a not-applicable unit is recorded" says exists precisely so every `BRx.y` this unit relies on reads as an inherited obligation rather than an unexplained reference — lists only `BR3.1–BR3.4` and never mentions `BR3.5` at all. `functional-spec.md`'s own accounting section is stale the same way: it names "the five requirements" governing U6 by ID (`FR1.5, FR2.5, FR3.3, NFR8, NFR9`) and does not mention `FR3.4`, which `traceability.json`'s `coverage` array (six entries) now carries. The rule and requirement that this pass's own evidence leans on for the Homelab liveUrl-omission check were added to `traceability.json` without being added to the sibling inventory documents (`rules.md`, `functional-spec.md`) that this same stage's contract says exist to keep every cited rule ID traceable to a named, explained entry. | Add `BR3.5` to `rules.md`'s governing-rules table with its authoring unit (U1) and what it requires, and update `functional-spec.md`'s "five requirements" accounting to name six (adding `FR3.4`) so the unit's own descriptive documents match what `traceability.json` actually asserts. | New |
| R-02 | Major | `construction/u6-launch-content/functional-design/functional-spec.md` was not modified by this claim; the claim itself appears in `code-generation/code-generation-questions.md` ([Q2] "The correction"), `code-generation/code-generation-plan.md` (§ "The correction this pass carries"), and `code-generation/code-summary.md` (§ "The `draft` correction, and its outcome" / § Step A2), each asserting: "Required fields are validated before the [draft] mark is read, so a malformed draft is still an error." | Verified directly against `src/content-source.ts`: the code comment these three artifacts paraphrase actually says "**Slug validity and front-matter parsing** are checked before the draft mark is read" (line ~234) — not "required fields." Tracing the actual control flow in `loadContent()`: only (a) the item directory containing `index.md`, (b) slug format validity, and (c) front-matter parsing as a key/value block happen before `readDraftMark()` is called and before a draft is dropped (`if (draft.isDraft) continue;`, ~line 321). The field-level requirements the three artifacts are actually talking about — `BR2.1–BR2.3` (title/summary/date) and `BR3.1–BR3.4` (name/summary/year/type/tools/repo) — are validated in `src/post-catalog.ts` and `src/project-catalog.ts`, which operate only on the `items` array `loadContent()` returns, and a draft item is never added to that array (it is `continue`d away before the `items.push(...)` a few lines later). A malformed draft — e.g. `draft: true` with no `date` — would therefore be silently dropped with no build failure at all, contradicting the three artifacts' claim that "a malformed draft is still an error." This is a technical over-statement, not a change to what shipped (neither content file carries a `draft` key), but it is asserted three times as the evidentiary basis for the correction this whole re-entry pass exists to make, and it does not hold up against the source it purports to summarize. | Correct the claim in all three artifacts to what the source comment actually supports (slug format and front-matter-parses-as-object are checked before the draft mark; per-field requirement validation runs later, only on non-draft items, so a malformed *draft's* missing/invalid required fields are never surfaced as a build error). | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| `npx tsc --noEmit` | Exit 0, no output | Clean |
| `npx eslint .` | Exit 0, no output | Clean |
| `npx prettier --check .` | Exit 0 — "All matched files use Prettier code style!" | Clean; confirms the plan's claim that the `.prettierignore` content exclusion works, independently reproduced |
| `npx vitest run tests/u1 tests/u2 tests/u3 tests/u4 tests/u5` | Exit 0 — 22 files, 160 tests, all passing | Matches `code-summary.md`'s recorded Step A3 output exactly (file count, test count, pass status) |
| `node --import tsx bin/check.ts` (not run by me; per plan/dispatch, this writes `dist/`/`.build/` and invalidates the review receipt) | Not run — recorded output scrutinized instead | `code-summary.md` § Step A2 records verbatim: build exit 0, "Built 11 pages into dist/ (4 from content files)"; all three blocking checks pass, exit 0. The recorded output is internally consistent with Step A4's built-output listing (9 files under `dist/`, including both of U6's two pages at their claimed permanent slugs) and with the direct `grep` evidence quoted for the Homelab rail's `Live` row omission. I did not re-run this command myself, consistent with the plan's § "How this pass is reviewed" and the dispatch instructions. |

### Independent checks performed

- **Empty `writes` array (`source-manifest.json`)**: verified true. Both content files' on-disk mtimes (2026‑09‑23) predate the second pass's other artifacts, and their content matches every claim in `code-summary.md` § "Step A1" — no `draft` key on either file, exactly the front-matter keys the unit-test-instructions and traceability files describe (project: `name, summary, year, type, tools, repo`; post: `title, summary, date`). No file under `content/` or `src/` shows any sign of this pass having written to it.
- **Mobbin exemption**: sound. `unit-of-work.md` § U6 records the unit's `Kind` as **untagged**, not `ui`, and its own boundary text ("Content files only: no templates, no build code, no styling") backs that. The unit produces two Markdown files and nothing else — no template, component, stylesheet, or build code — which is exactly the condition `project.md` § Corrections states for the exemption. Not a convenient dodge.
- **The "there is no draft state" correction**: the correction itself is accurate. `src/content-source.ts` does implement an optional boolean `draft` key (`readDraftMark`, actual function starts at line 141/effective read at 142) that is dropped from everything downstream (`continue` at line 321) — the mechanism the first pass denied does exist. (See R-02 for where the artifacts overstate a related but different claim about *when* that mechanism runs relative to field validation.)
- **Content-model conformance**: both files carry all required fields with no missing or malformed values (Homelab: `name, summary, year, type, tools, repo`, `liveUrl` correctly omitted per U6CA1; post: `title, summary, date`), matching `functional-spec.md`'s stated field-set constraints and confirmed independently by direct file inspection, not merely by trusting the recorded build output.
- **Coverage floor**: `code-summary.md` and `code-generation-plan.md` both record `N/A — no instrumentable lines in this unit` with the reason stated, never as a pass, matching `team.md` § Testing Posture's requirement exactly.

### Summary

The two content files are sound, the empty `source-manifest.json` is truthful, the Mobbin exemption is legitimate rather than a dodge, and the coverage floor is recorded correctly as N/A rather than papered over. The re-entry's central "there is no draft state" correction is itself accurate. Against that, this pass's own traceability additions (`FR3.4`/`BR3.5`, added to justify the Homelab liveUrl-omission evidence) were not propagated into the unit's sibling inventory documents (`rules.md`, `functional-spec.md`), and the pass repeats — in three separate artifacts — a claim about when required-field validation runs relative to the draft mark that overstates what `src/content-source.ts` actually does, verified by tracing the control flow directly. Both are Major, evidence-grounded, and worth fixing before this pass is taken as the final word on the draft-mechanism question it exists to settle, but neither breaks what has actually shipped (two published, fully-conformant, non-draft content files) or exceeds the two-Major threshold for blocking readiness.

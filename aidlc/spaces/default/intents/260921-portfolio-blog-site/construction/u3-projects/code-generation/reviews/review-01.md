## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-23T18:27:58Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Minor | `src/page-renderer/pages.ts` lines 86, 89, 257, 263, cross-checked against `code-summary.md` § "`data-testid` on the four project anchors" and every file under `tests/u3/` | `code-summary.md` states the four `data-testid` attributes (`project-row-name-link`, `project-row-repo-link`, `project-rail-repo-link`, `project-rail-live-link`) were added as "stable … hooks" for test resilience across the heading-level change. `grep -rn "data-testid" tests/u3/ tests/u1/ tests/u2/` returns zero matches — no test queries by the `data-testid` attribute name. A substring search shows only one of the four values (`project-row-repo-link`) is referenced anywhere in the suite, and only incidentally: `page-renderer.test.ts` line 108 filters the full anchor-tag string by `.includes("project-row-repo-link")`, which happens to match because that string only occurs inside the `data-testid` value, not because the test was written to select by that attribute (every other assertion in the suite locates markup by class name, `href`, or `aria-label`). The other three attributes (`project-row-name-link`, `project-rail-repo-link`, `project-rail-live-link`) are dead production markup, shipped to every reader on every project row and project page, unreferenced by anything. Neither `code-generation-plan.md` nor `unit-test-instructions.md` calls for this attribute set — it was introduced during implementation and its stated rationale does not match what was delivered. | Either wire the tests to actually select by `data-testid` (making the claimed rationale true), or remove the three unused attributes and correct `code-summary.md`'s description to state accurately what, if anything, the remaining attribute is for. | New |
| R-02 | Minor | `traceability.json` NFR6 row, cross-checked against `requirements.md` line 169 (NFR6 criterion: "Load each page type at a desktop width and at a phone width; each stated behaviour holds") and `tests/u3/page-renderer.test.ts` BR9.5 tests | `traceability.json` marks NFR6 `"status": "OK"`, but no test in this unit loads a page at a desktop or phone width or asserts any width-dependent behaviour — the cited target (`page-renderer.test.ts`) only asserts that the rail and body are DOM siblings, which `code-summary.md` itself correctly describes as "the structural commitment U5 relies on to stack the rail above the body," i.e. a precondition for NFR6, not NFR6 itself. This unit's own plan is explicit elsewhere that visual/responsive behaviour is U5's ("No styling" — `code-generation-plan.md` § What this plan does not do), and the sibling requirement NFR7 (also styling-dependent, also owned by U5's BR11.1) is honestly recorded as `"status": "Deferred"` with that reasoning spelled out. NFR6 does not receive the same honest treatment; it is marked fully satisfied. | Change NFR6's status to `Deferred` (or `Partial`) with the same reasoning already used for NFR7, naming the structural precondition this unit actually delivers and the U5 dependency for the rest. | New |
| R-03 | Minor | `u3-projects/functional-design/frontend-components.md` § Props and branches (`ProjectRow \| project \| none`), cross-checked against `src/page-renderer/pages.ts` `projectRow(project, headingLevel)` and `code-generation-plan.md` § "Where the fix goes" | The delivered `ProjectRow` now takes a second parameter, `headingLevel`, not recorded in the unit's own upstream functional-design contract, which still lists `project` as its only prop and `none` for its branches. The plan is transparent that this is a deliberate, approved extension (assumption `U3CA1`, accepted at the Plan Approval gate), so this is not a fabricated or unauthorized change — but the artifact of record that a future construction unit (U4, U5, U6) would read to learn `ProjectRow`'s prop surface is now stale and would need to be cross-referenced against the code-generation output to get the real signature. | Note the added prop in `frontend-components.md` (or in a durable cross-reference from it to this unit's code-generation output) so the component contract stays accurate for units built after this one. | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| `npm run typecheck` (transcript) | exit 0 | No type errors introduced by the `RowHeadingLevel` prop addition. |
| `npm run lint` (transcript) | exit 0 | Consistent with the codebase's existing style. |
| `npm run format:check` (transcript) | exit 0 | Formatting clean. |
| `npm run build` (transcript) | exit 0, 9 pages | Build unaffected. |
| `npm run check` (transcript) | 3/3 blocking checks pass | Site builds, every content file produced a page, internal links resolve — matches the three-check regime `team.md` mandates. |
| `npx vitest run tests/u3 --coverage` (transcript) | 4 files, 24 tests pass; `pages.ts` 100% lines | Matches `code-summary.md`'s claimed file/test counts exactly (6+9+4+5=24) and matches the per-file breakdown read from the actual test files. |
| `npx vitest run tests/u1 tests/u2 tests/u3 --coverage` (transcript) | 17 files, 133 tests pass; 96.17% lines against the 80% floor | Confirms the combined-run coverage claim in `code-summary.md`; U1's 82 and U2's 27 tests both stay green, as the plan required. |
| Manual: fixture front matter vs. claimed ordering | `beacon` (featured) → `cartograph` (2026) → `almanac` (2025) | Matches `EXPECTED_ORDER` in `integration.test.ts` and the plan's ordering rule (BR9.3: featured first, then year descending). |
| Manual: `source-manifest.json` vs. `find tests/u3/fixtures` and repository file listing | Exact match, no extra or missing files | No unrelated or undeclared files were touched by this unit. |
| Manual: `traceability.json` IDs vs. `requirements.md` | All BR9.x, FR3.x, NFR1/6/7/9/11/12 IDs resolve to real requirements | No dangling or invented requirement references. |
| Manual: `data-testid` attribute usage vs. test suite | 3 of 4 attributes unused; 1 used only incidentally | Grounds finding R-01. |

### Summary

The plan's central factual claim — that four of U3's five business rules were already correctly implemented by U1 and that the one real defect was a heading-level skip on the Projects page — holds up against the actual source, and the fix (a `headingLevel` prop threaded through the one shared row include) is minimal, correctly wired (`renderProjects` passes `2`, `renderHome` passes `3`), and covered by tests that were shown, by the artifact's own account, to fail against the pre-fix behaviour. Every test-count, coverage, and fixture-ordering claim in `code-summary.md` and `traceability.json` was independently checked against the actual test files, fixtures, and verification transcript and found accurate, with the three exceptions above: unused `data-testid` markup described as load-bearing when it mostly is not, one NFR marked fully satisfied when only its structural precondition is tested, and a stale upstream component contract. None of these three findings represents a broken reference, a circular dependency, or a defect a developer building on this unit would be blocked by — they are documentation-accuracy and cleanliness gaps, correctly scoped as Minor.

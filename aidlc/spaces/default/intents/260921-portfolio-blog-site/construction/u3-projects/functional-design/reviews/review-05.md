## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-23T23:12:02Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | `construction/u3-projects/functional-design/functional-spec.md` \| opening paragraph ("It carries no entity model and no base rule block...") | The stage definition (`.claude/aidlc-common/stages/construction/functional-design.md` § Step 4, `functional-spec.md` bullet) states that for a UI-only unit which produces `functional-spec.md` without `entities.md`/`rules.md`, that file "is self-contained: it authoritatively specifies the interaction workflows and screen/state transitions from the unit definition and requirements, with no entity/rule dependency." U3 is exactly that case (`produces_kinds` excludes `entities`/`rules` for kind `ui`), yet `functional-spec.md` explicitly and repeatedly depends on another unit's construction artifacts for its foundational content \\u2014 the `Project` entity and rules `BR1.x`, `BR3.x`, `BR4.x`, `BR5.x` are all cited as authored by `construction/u1-publishable-site-shell/functional-design/entities.md` and `rules.md`, and BR9.1\\u2013BR9.5 are stated as building "on top of" those rules rather than restating them. The walking-skeleton team learning (`project.md` \\u00a7 Corrections, units-generation) explains *why* U1 pre-populates every component minimally, which mitigates this, but the artifact never states the tension against the stage contract's literal self-containment promise, so a reader relying on the produced-artifact contract (that a `ui` unit's spec needs no external entity/rule file) will be surprised that this one does. | Add an explicit note in `functional-spec.md` (or in the questions file's "What was subtracted" table) naming the stage-template expectation of self-containment for `ui` units, and stating explicitly that this unit deliberately departs from it because of the affirmed walking-skeleton practice, per the team's own learned rule that a divergence from an affirmed practice should be recorded with its cost rather than left implicit. | New |
| R-02 | Minor | `construction/u3-projects/functional-design/functional-spec.md` \\u00a7 "What this unit completes from U1", row `FR4.3` | The table claims this unit "completes" `FR4.3` ("That section filled with real entries"), but `traceability.json`'s `upstream_ids`/`coverage` (correctly) contains only `FR3.1`\\u2013`FR3.7`, and `unit-of-work.md` \\u00a7 Coverage states "every functional requirement... is delivered by exactly one unit." It is unclear from the artifact whether U3 is claiming a second, untracked contribution to a single-owner requirement, or merely describing an incidental side effect of `ProjectCatalog` now returning real ordered data (which needs no separate traceability entry). | Clarify the FR4.3 row: either state plainly that this is an incidental side effect requiring no traceability entry (and soften "completes" to something like "incidentally populates"), or, if it is treated as a genuine second contribution, add it to `traceability.json` and reconcile with the single-owner statement in `unit-of-work.md`. | New |
| R-03 | Minor | `construction/u3-projects/functional-design/functional-design-questions.md` \\u00a7 Q1, and `functional-spec.md` rule `BR9.3` rationale | Q1 frames "whether the full Projects page uses the same order as Home" as a still-open decision with four live alternatives (A\\u2013D), reasoning that U1's `BR3.7` "defined it to serve Home, which shows only the top three." But `components.md` describes `ProjectCatalog` as exposing a single ordered project list, and `PageRenderer`'s one dependency edge on `ProjectCatalog` is described as feeding "the Projects list, Home, and project pages" together \\u2014 not two separately-scoped orderings. `unit-of-work.md` \\u00a7 U3 likewise states the order (featured, then year desc, then slug asc, per ADR-008) as already settled for `ProjectCatalog` itself, without scoping it to Home's top three. Read against those two upstream contracts, options B/C/D may have already been foreclosed by Domain Design before this question was asked, which would make Q1's framing of "a separate decision" and BR9.3's framing of "extending" a narrower rule inaccurate rather than merely stylistic. | Confirm against `inception/domain-design/decisions.md` ADR-008 (out of this review's scope) whether the full-catalogue order was already settled independent of Home's subset-selection, and adjust Q1's/BR9.3's framing accordingly so a later reader does not believe alternatives B\\u2013D were live when they may not have been. | New |
| R-04 | Minor | `construction/u3-projects/functional-design/frontend-components.md` \\u00a7 Responsive commitments, "How the outbound link actually reaches 44px is U5's rule" | This unit names a specific cross-unit integration point (U5's `BR11.1`/`BR11.6`) and records it here, in the depending unit, as the team's own learned correction requires ("record it in the owning unit, in each depending unit, and in the integration-points table"). This review's scope forbids opening U5's files or `unit-of-work-dependency.md` to confirm the other two legs of that three-way recording, so the dependency's completeness could not be verified here. | Confirm, outside this review, that `unit-of-work-dependency.md` \\u00a7 Integration points and U5's own functional design both name this same U3\\u2194U5 dependency, so the three-way recording the team affirmed is actually in place. | New |

### Validation Tool Results

No validation tools are listed for the `functional-design` stage beyond the sensors (`required-sections`, `upstream-coverage`, `linter`, `type-check`, `traceability`), which are gate-fired automated checks rather than a shell tool this review invokes directly. No shell-runnable validator was available to run for this unit's artifacts within this review's scope.

### Summary

The artifact is internally consistent on the load-bearing points a developer would build from: the four traceability rows map cleanly onto `BR9.1`\\u2013`BR9.5`, the two-target row shape and the metadata-rail omit rule are unambiguous and match `requirements.md` FR3.1\\u2013FR3.6 and `components.md`'s `ProjectCatalog`/`PageRenderer` boundaries, and the unit correctly declines to redefine the `Project` entity or its validation rules. The Major finding (R-01) is a genuine tension against the stage template's literal self-containment promise for `ui` units, softened but not eliminated by the affirmed walking-skeleton practice; the remaining findings are documentation/traceability clarity gaps rather than defects that would block a developer from implementing this unit correctly. With one Major and no Critical findings, the design is implementable as written.

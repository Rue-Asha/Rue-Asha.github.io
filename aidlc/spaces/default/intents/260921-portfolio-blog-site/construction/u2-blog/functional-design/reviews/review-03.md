## Review

**Verdict:** READY
**Reviewer:** aidlc-architecture-reviewer-agent
**Date:** 2026-09-23T13:37:39Z
**Iteration:** 1

### Findings

| ID | Severity | Location | Finding | Required action | Status |
|---|---|---|---|---|---|
| R-01 | Major | `construction/u2-blog/functional-design/traceability.json` > coverage rows for `FR2.1`, `FR2.3`, `FR2.4` | Three `status: OK` rows cite `BR` targets that do not actually establish the requirement's content. `FR2.3` ("each Writing list row a single link target covering title, summary, and date") targets `BR2.4`, which is the post-ordering rule (`functional-spec.md` line 41: "date descending, ties by slug (U1 BR2.4)") and has no logical connection to single-link-row behaviour — that behaviour is asserted only in workflow step W1.4 and in `frontend-components.md` § "The whole row is one link", neither of which is a `BR`. `FR2.1` ("Writing page ... each entry showing title, summary, and date") targets `BR2.4` (ordering) + `BR5.5` (empty state) — both real but neither says anything about what a row displays; that content again lives only in W1.3 / the `PostRow` props table. `FR2.4` ("a page per published post carrying title, summary, date, and body") targets `BR2.1`–`BR2.3`, which per this unit's own subtraction table (`functional-design-questions.md` § "What was subtracted") are the field-*validation* rules ("a missing or unparseable field fails the build"), not a rule that the post page renders those fields — that is W2, not a `BR`. The `sensor-traceability` run against this file also fails closed on every target (`"target BRx.y is absent from rules.md"`) because `u2-blog` produces no `rules.md`, per the dispatch's documented limitation (a) — that failure mode is not being raised here; what is being raised is that even once the sensor's rules-file lookup is repointed at `functional-spec.md`'s embedded `rules:` block, three of these ten coverage rows would still cite targets that do not semantically cover their requirement. | Either retarget `FR2.1`, `FR2.3`, and `FR2.4`'s coverage rows at the actual mechanism (the relevant `W`-step / component-props reference, if the traceability schema is meant to allow that for a `ui` unit with no `rules.md`), or add the missing `BR`s that state the row-content and page-rendering behaviour explicitly so the existing `BRx.y` targets are true. | New |
| R-02 | Minor | `construction/u2-blog/functional-design/frontend-components.md` § "Props and branches" note on `posts` ordering | The note says post ordering "is the one piece of this unit [U2] that has a unit test behind it," while attributing the rule itself to "U1 BR2.4" throughout `functional-spec.md`. `inception/units-generation/unit-of-work.md` § U2 assigns U2 clear ownership ("Owns `PostCatalog`'s rules"), so Build-and-Test responsibility for the ordering unit test required by `team.md` § Testing Posture ("post sorting" needs a unit test) does sit with U2 — but nothing in either U2 artifact states plainly that despite the rule's `BR2.4` number and text living in U1's `rules.md` (walking-skeleton pattern), the component it governs (`PostCatalog`) and the unit test obligation are U2's, not U1's, to verify at Build and Test. | Add one sentence tying the `BR2.4` cross-reference explicitly to U2's Build-and-Test obligation, so a reader does not read "U1 BR2.4" as "U1 also owns verifying it." | New |
| R-03 | Minor | `construction/u2-blog/functional-design/functional-spec.md` § Sources | `NFR12` (the per-unit line-coverage floor / `N/A` recording requirement) is listed as a consumed upstream requirement but never referenced again in the body — unlike `NFR2` and `NFR7`, which are both cited at their point of use (W1, W3). | Either drop `NFR12` from Sources if it is only a generic Build-and-Test concern with nothing unit-specific to say here, or add one line noting which U2 components carry instrumentable code (the post-facing parts of `ContentTransforms`, `MarkupRenderer`) so the NFR12 floor has a named target for this unit. | New |

### Validation Tool Results

| Tool | Result | Interpretation |
|---|---|---|
| `sensor-required-sections` (`functional-spec.md`) | PASS — 6 H2 headings found | Matches the stage's required structure. |
| `sensor-upstream-coverage` (`functional-spec.md`) | PASS — no `consumes` declared for this stage/file pairing in this sensor's resolver | No gap surfaced; does not by itself confirm semantic coverage (see R-01). |
| `sensor-traceability` (`traceability.json`, `--stage functional-design`) | FAIL — `invalid_targets`: every `BRx.y` target reported "absent from rules.md" (17 entries); `missing_from_upstream_ids`: every FR/NFR this unit does not carry (28 entries) | Both failure classes match the dispatch's pre-diagnosed, out-of-scope limitations (a) and (b) exactly — `u2-blog` is a `ui` unit with no `rules.md` (its rules live in `functional-spec.md`'s embedded YAML block instead), and the sensor's upstream-ID resolver only scopes per-unit when `inception/user-stories/stories.md` exists, which this scope does not produce. Not raised as a finding. No `orphans` were reported. |

### Summary

The workflows, screen-state model, and the two new code-fence/feed rules are coherent, implementable, and consistent with the affirmed testing and accessibility posture — BR8.1's "authoring rule the build does not check" is stated honestly against the declined accessibility scan, and BR8.2's known/unknown-language split has a concrete PRIMARY/FALLBACK resolution mechanism. The main defect found is traceability accuracy: three `OK` coverage rows (`FR2.1`, `FR2.3`, `FR2.4`) cite `BR` targets that do not actually state the content being claimed as covered, most starkly `FR2.3` pointing at the unrelated ordering rule `BR2.4`. That is a correctness gap in the deliverable itself, not merely a sensor limitation, but it does not block implementation — the real behaviour is fully specified elsewhere in the same file (workflows, component props) — so it is recorded as Major rather than Critical. With one Major and two Minor findings and no Critical, the artifact is READY.
